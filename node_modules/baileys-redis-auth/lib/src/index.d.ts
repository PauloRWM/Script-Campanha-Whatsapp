import { RedisOptions, Redis as RedisClient } from 'ioredis';
import { AuthenticationState } from '@whiskeysockets/baileys';
interface IDeleteHSetKeyOptions {
    redis: RedisClient;
    key: string;
}
interface IDeleteKeysOptions {
    redis: RedisClient;
    pattern: string;
}
export declare const useRedisAuthStateWithHSet: (redisOptions: RedisOptions, prefix?: string) => Promise<{
    state: AuthenticationState;
    saveCreds: () => Promise<void>;
    redis: RedisClient;
}>;
export declare const deleteHSetKeys: ({ redis, key }: IDeleteHSetKeyOptions) => Promise<void>;
export declare const useRedisAuthState: (redisOptions: RedisOptions, prefix?: string) => Promise<{
    state: AuthenticationState;
    saveCreds: () => Promise<void>;
    redis: RedisClient;
}>;
export declare const deleteKeysWithPattern: ({ redis, pattern, }: IDeleteKeysOptions) => Promise<void>;
export {};
